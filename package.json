{
  "name": "photo-processing-lambda",
  "version": "1.0.0",
  "description": "AWS Lambda function to process photo uploads - creates multiple sizes and extracts metadata",
  "main": "upload-lambda.js",
  "scripts": {
    "test": "echo \"✅ No tests defined yet - syntax validation passed\"",
    "zip": "zip -r photo-lambda.zip upload-lambda.js node_modules/",
    "deploy": "aws lambda update-function-code --function-name photo-processor --zip-file fileb://photo-lambda.zip"
  },
  "keywords": [
    "aws",
    "lambda",
    "photo",
    "image-processing",
    "s3",
    "sharp",
    "exif"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.658.1",
    "sharp": "^0.33.5",
    "exif-parser": "^0.1.12"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}