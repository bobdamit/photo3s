{
  "name": "photo-processing-lambda",
  "version": "1.0.0",
  "description": "AWS Lambda function to process photo uploads - creates multiple sizes and extracts metadata",
  "type": "module",
  "main": "handler/src/upload-lambda.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watchAll",
    "test:coverage": "jest --coverage"
  },
  "keywords": [
    "aws",
    "lambda",
    "photo",
    "image-processing",
    "s3",
    "sharp",
    "exif"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.658.1",
    "exif-parser": "^0.1.12",
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "aws-sdk-client-mock": "^4.1.0",
    "aws-sdk-mock": "^6.2.1",
    "jest": "^29.7.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
